# คู่มือการใช้งานระบบ S-Link (Mobile Application)
# S-Link User Manual

แอปพลิเคชัน S-Link เป็นระบบบริหารจัดการบนมือถือ (Mobile POS & Logistics) ที่ถูกออกแบบมาให้เชื่อมต่อกับระบบ POS หน้าจอหลัก (Desktop) เพื่อกระจายการทำงานไปยังพนักงานหลายส่วน เช่น ฝ่ายขายหน้าร้าน, ผู้ดูแลระบบ, คนขับรถขนส่ง และเด็กปั๊ม โดยแบ่งตามสิทธิ์ผู้ใช้งาน (Roles)

---

## 1. การเริ่มต้นใช้งานและการตั้งค่า (Getting Started & Settings)

### 1.1 การตั้งค่าระบบ API (API Configuration)
> [!IMPORTANT]
> **สำคัญมาก:** แอปพลิเคชันจำเป็นต้องตั้งค่า API URL ก่อนใช้งาน เพื่อให้สามารถเชื่อมต่อกับฐานข้อมูลหลักที่ร้านได้
1. ผู้ดูแลระบบ (Admin) หรือพนักงานหน้าร้าน (Requester) ล็อกอินเข้าสู่ระบบ
2. ไปที่เมนู **ตั้งค่า (Settings)** เลื่อนลงมาที่ **ตั้งค่าระบบ POS (POS Config)**
3. เลือก **ตั้งค่าการเชื่อมต่อ (API)** และกรอก URL ของ Cloudflare Tunnel (เช่น `https://api.yourdomain.com`)
4. กดบันทึก (Save)
*หมายเหตุ: สำหรับเครื่องของคนขับรถ (Driver) ปัจจุบัน Admin จำเป็นต้องล็อกอินไปตั้งค่า API ให้ก่อนในครั้งแรก จากนั้นจึงสลับบัญชีให้คนขับเข้าใช้งาน*

### 1.2 การตั้งค่าทั่วไป (General Settings)
- **โหมดกลางคืน (Dark Mode):** สามารถเปิด/ปิดได้ตามต้องการที่หน้า Settings 
- **การแจ้งเตือน (Notifications):** สามารถเลือกเสียงแจ้งเตือนได้ในส่วนการตั้งค่าทั่วไป

---

## 2. สำหรับผู้ดูแลระบบ (Admin)

### 2.1 การอนุมัติผู้ใช้งาน (User Approval)
1. ไปที่เมนู **ตั้งค่า** > **Approve Users**
2. ระบบจะแสดงรายชื่อผู้ที่สมัครสมาชิกใหม่ (สถานะ Pending)
3. เลือกสิทธิ์ (Role) ให้พนักงาน (เช่น Driver, Requester, Gas Station, HR, Admin)
4. เมื่อกดอนุมัติ ระบบจะบันทึกข้อมูลและสามารถล็อกอินได้ทันที (หากเป็น Driver/Requester ระบบจะเพิ่มชื่อเข้าฐานข้อมูลคนขับรถอัตโนมัติ)

### 2.2 แดชบอร์ดสถิติคนขับรถ (Driver Stats Dashboard)
- **การเข้าถึง:** แตะที่เมนู **สถิติการส่งของ** 
- **การแสดงผล:** ระบบจะคำนวณจากประวัติการทำงาน 50 งานล่าสุด แยกเป็นแท็บ "คนทำงาน" และ "รถขนส่ง" เพื่อดูว่าใครหรือรถคันไหนรับงานบ่อยสุด แสดงผลแบบจัดอันดับพร้อมหลอดเปอร์เซ็นต์

### 2.3 การจัดการพนักงาน (Manage Employees)
- สามารถแก้ไขชื่อ หรือ ปรับเปลี่ยนตำแหน่ง (Role) ได้ตลอดเวลา
- สามารถลบพนักงานออกจากระบบได้ (สิทธิ์การเข้าถึงจะถูกตัดทันที)

---

## 3. สำหรับพนักงานจัดของและหน้าร้าน (Requester)

ผู้ใช้ในกลุ่มนี้สามารถเข้าถึงฟีเจอร์การขายและจัดการสต็อกได้เหมือนพกเครื่อง POS เดินตรวจในร้าน

### 3.1 การใช้งาน Mini POS (รับชำระเงิน)
- **การเพิ่มสินค้า:** ค้นหาสินค้าจากชื่อ หรือใช้กล้องมือถือสแกนบาร์โค้ด
- **การชำระเงิน (Payment):** รองรับการรับเงินสด (พร้อมคำนวณเงินทอน) และสร้าง QR Code PromptPay ให้ลูกค้าสแกนได้ทันที
- **การจัดส่งด่วน (Delivery):** สามารถกดส่งสินค้าแบบด่วน ระบบจะส่งออเดอร์ไปหน้ารถทันที และแจ้งเตือนลูกค้าทาง LINE

### 3.2 การจัดการสต็อก (Stock Check & Receive)
- **นับสต็อก (Stock Check):** สแกนบาร์โค้ดสินค้าเพื่อดู "ยอดในระบบ" เทียบกับ "ยอดที่นับได้จริง" ระบบจะแสดงผลต่าง (Diff) ชัดเจน เมื่อยืนยันยอดจะอัปเดตทันที
- **รับของเข้า (Stock Receive):** สแกนสินค้าและกรอกจำนวนที่รับเข้าเพื่อบวกสต็อกเพิ่ม

---

## 4. สำหรับคนขับรถขนส่ง (Driver)

ออกแบบมาให้ใช้งานง่าย (Lazy-Friendly) และลดขั้นตอนความยุ่งยากระหว่างการจัดส่ง

### 4.1 การรับงานและนำทาง (Job Dashboard & Routing)
- เมื่อล็อกอินเข้าแอป จะพบกับหน้า **"งานส่งของ" (Jobs)** ซึ่งแบ่งเป็น "งานมาใหม่" และ "งานกำลังดำเนินการ"
- **การนำทาง (Navigation):** ในหน้ารายละเอียดงาน จะมีปุ่มสีเขียว **"นำทางด้วย GPS"** (ถ้าลูกค้าเคยมีพิกัดแล้ว) หรือปุ่มสีฟ้า **"ค้นหาแผนที่จากชื่อ"** เพื่อเปิด Google Maps นำทางอัตโนมัติ

### 4.2 การปิดงาน (Complete Job) และ การเก็บเงิน (COD)
- เมื่อถึงที่หมาย คนขับต้องกดปุ่มปิดงาน และ **ระบบบังคับให้ถ่ายรูปหลักฐานการจัดส่ง** 
- **พิกัดอัตโนมัติ:** ระบบจะตั้งค่าให้ติ๊กถูกที่ "บันทึกพิกัดปัจจุบันเป็นที่อยู่ลูกค้า" ไว้ล่วงหน้าเสมอ เพื่อความสะดวกในรอบถัดไป
- **การเก็บเงินปลายทาง (COD):** หากเป็นบิลค้างชำระ ระบบจะเด้งกล่องสีส้มขึ้นมา เมื่อคนขับกดรับเงิน **ระบบจะพิมพ์ตัวเลขยอดเงินที่ต้องเก็บให้โดยอัตโนมัติ** (คนขับไม่ต้องกดตัวเลขเอง ยกเว้นลูกค้าจ่ายไม่ครบ)

---

## 5. สำหรับพนักงานปั๊มน้ำมัน (Gas Station)

ออกแบบมาเพื่อใช้เป็นจุดเช็คอินพิกัดการทำงานของเด็กปั๊มโดยเฉพาะ

### 5.1 การบันทึกเวลาเข้า-ออกงาน (Attendance)
- ระบบจะแสดงหน้าปุ่มกดเข้างาน (Clock In) / ออกงาน (Clock Out) เท่านั้น
- **ระบบป้องกันการโกงเวลา:** มีการทำงานร่วมกับระบบ GPS หากพนักงานไม่ได้อยู่ในรัศมีของปั๊ม/ร้านค้าตามที่ตั้งค่าไว้ จะไม่สามารถกดเข้า-ออกงานได้ (แจ้งเตือน "คุณไม่ได้อยู่ที่ร้าน")
- *(หมายเหตุ: ยังไม่มีระบบบันทึกปริมาณน้ำมันผ่านแอปในเวอร์ชันปัจจุบัน)*
